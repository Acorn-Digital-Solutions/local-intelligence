{
  "$comment": "Ollama provider template for OpenCode (local models only). Merged by phase4.sh into ~/.config/opencode/opencode.jsonc under provider.ollama — your other keys are never touched. The models block is built dynamically from `ollama list` (embedding-only models skipped, every AGENT_MODELS entry flagged tool_call); everything a human would tweak lives here.",
  "npm": "@ai-sdk/openai-compatible",
  "name": "Ollama (local)",
  "options": {
    "baseURL": "http://127.0.0.1:11434/v1"
  },
  "model_name_suffix": " (local)",
  "agentic_name_suffix": " (local, agentic)",
  "default_limit": {
    "context": 32768,
    "output": 8192
  },
  "skip_substrings": ["embed", "bge-m3", "mxbai"]
}
